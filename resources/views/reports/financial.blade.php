@extends('layouts.app')
@section('title', __('app.financial_statement'))
@section('content')
<div class="mb-3 d-flex gap-2 flex-wrap"><a class="btn btn-success" href="{{ route('reports.export', 'financial') }}">{{ __('app.export_excel') }}</a><a class="btn btn-outline-secondary" href="{{ route('reports.members') }}">{{ __('app.member_report') }}</a><a class="btn btn-outline-secondary" href="{{ route('reports.savings') }}">{{ __('app.savings_report') }}</a><a class="btn btn-outline-secondary" href="{{ route('reports.loans') }}">{{ __('app.loan_report') }}</a><a class="btn btn-outline-secondary" href="{{ route('reports.installments') }}">{{ __('app.installment_report') }}</a></div>
<div class="row g-4 mb-4"><div class="col-md-4"><div class="card stat-card"><div class="card-body"><div class="text-muted">{{ __('app.total_income') }}</div><h4>Rp {{ number_format($summary['income'], 0) }}</h4></div></div></div><div class="col-md-4"><div class="card stat-card"><div class="card-body"><div class="text-muted">{{ __('app.total_expense') }}</div><h4>Rp {{ number_format($summary['expense'], 0) }}</h4></div></div></div><div class="col-md-4"><div class="card stat-card"><div class="card-body"><div class="text-muted">{{ __('app.net_profit') }}</div><h4>Rp {{ number_format($summary['income'] - $summary['expense'], 0) }}</h4></div></div></div></div>
<div class="row g-4"><div class="col-lg-6"><div class="card panel"><div class="card-body"><h5>{{ __('app.profit_loss') }}</h5><table class="table"><thead><tr><th>{{ __('app.account') }}</th><th>{{ __('app.debit') }}</th><th>{{ __('app.credit') }}</th></tr></thead><tbody>@foreach($profitLoss as $account)<tr><td>{{ $account->code }} - {{ $account->name }}</td><td>{{ number_format($account->debit_total ?? 0, 0) }}</td><td>{{ number_format($account->credit_total ?? 0, 0) }}</td></tr>@endforeach</tbody></table></div></div></div><div class="col-lg-6"><div class="card panel"><div class="card-body"><h5>{{ __('app.balance_sheet') }}</h5><table class="table"><thead><tr><th>{{ __('app.account') }}</th><th>{{ __('app.debit') }}</th><th>{{ __('app.credit') }}</th></tr></thead><tbody>@foreach($balanceSheet as $account)<tr><td>{{ $account->code }} - {{ $account->name }}</td><td>{{ number_format($account->debit_total ?? 0, 0) }}</td><td>{{ number_format($account->credit_total ?? 0, 0) }}</td></tr>@endforeach</tbody></table></div></div></div></div>
@endsection
