@extends('layouts.app')
@section('title', __('app.loan_management'))
@section('content')
<div class="d-flex justify-content-end mb-3"><a class="btn btn-primary" href="{{ route('loans.create') }}">{{ __('app.new_loan') }}</a></div>
<div class="card panel"><div class="card-body table-responsive"><table class="table table-hover"><thead><tr><th>{{ __('app.application_no') }}</th><th>{{ __('app.member') }}</th><th>{{ __('app.amount') }}</th><th>{{ __('app.interest') }}</th><th>{{ __('app.period') }}</th><th>{{ __('app.status') }}</th><th>{{ __('app.remaining') }}</th><th></th></tr></thead><tbody>@forelse($loans as $loan)<tr><td>{{ $loan->application_number }}</td><td>{{ $loan->member->name }}</td><td>Rp {{ number_format($loan->amount, 0) }}</td><td>{{ $loan->interest_rate }}%</td><td>{{ $loan->installment_period }} mo</td><td><span class="badge text-bg-warning">{{ __('app.'.$loan->status) }}</span></td><td>Rp {{ number_format($loan->remaining_balance, 0) }}</td><td class="text-end d-flex gap-2 justify-content-end"><a class="btn btn-sm btn-outline-secondary" href="{{ route('loans.show', $loan) }}">{{ __('app.view') }}</a><a class="btn btn-sm btn-outline-primary" href="{{ route('loans.edit', $loan) }}">{{ __('app.edit') }}</a>@if(auth()->user()->hasAnyRole(['admin','manager']) && $loan->status === 'pending')<form method="POST" action="{{ route('loans.approve', $loan) }}">@csrf<button class="btn btn-sm btn-success">{{ __('app.approve') }}</button></form><form method="POST" action="{{ route('loans.reject', $loan) }}">@csrf<button class="btn btn-sm btn-outline-danger">{{ __('app.reject') }}</button></form>@endif<form method="POST" action="{{ route('loans.destroy', $loan) }}">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger" onclick="return confirm('{{ __('app.delete') }}?')">{{ __('app.delete') }}</button></form></td></tr>@empty<tr><td colspan="8" class="text-center text-muted">{{ __('app.no_loans_found') }}</td></tr>@endforelse</tbody></table></div></div>
<div class="mt-3">{{ $loans->links() }}</div>
@endsection
